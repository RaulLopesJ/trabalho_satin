# Evidências — T06 Railway

Capture e preserve:

1. request e response JSON completos;
2. `usage.json` com `promptTokenCount`, `candidatesTokenCount` e demais campos disponíveis;
3. `cost.md` e `evaluation.md`;
4. árvore de arquivos do output, incluindo `railway.json`, `start.sh` e `package.json` raiz;
5. resultado de validação do JSON;
6. logs de `npm install`, `npm run build`, `/health` e deploy Railway;
7. screenshot do erro anterior, quando usado como diagnóstico, e screenshot do deploy corrigido.

Não registre chaves, tokens ou dados pessoais. Um deploy só pode ser marcado como comprovado com log/URL verificável.
