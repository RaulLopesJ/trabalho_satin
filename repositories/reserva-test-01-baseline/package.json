{
  "name": "reserva-test-01-baseline",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicação de reservas Hospetse — test-01 baseline",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm --workspace frontend run build",
    "start": "npm --workspace backend start"
  }
}
