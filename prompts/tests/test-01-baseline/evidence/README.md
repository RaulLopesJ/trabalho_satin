# Evidências — T01

Salve aqui prints ou exports que comprovem o modelo/configuração, a execução, os tokens e a resposta. Use nomes como `T01-run-01-model.png`, `T01-run-01-usage.png` e `T01-run-01-response.png`.

Não salve credenciais. A evidência deve corresponder ao run ID registrado em `executions/`.
