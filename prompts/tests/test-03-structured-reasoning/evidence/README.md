# Evidências — T03

Salve aqui prints ou exports que comprovem o modelo/configuração, a execução, a instrução de raciocínio estruturado, os tokens e a resposta. Use nomes como `T03-run-01-model.png`, `T03-run-01-usage.png` e `T03-run-01-response.png`.

Não salve credenciais. A evidência deve corresponder ao run ID registrado em `executions/`.
